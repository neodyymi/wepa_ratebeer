<h2>Create new style</h2>


<%= form_for(@style) do |f| %>
    <% if @style.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@styl.errors.count, "error") %> prohibited rating from being saved:</h2>

          <ul>
            <% @style.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    Style: <%= f.text_field :style %>
    <br/>
    Style: <%= f.text_area :description %>
    <br/>
    <%= f.submit %>

<% end %>